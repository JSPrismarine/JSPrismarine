save-exact=true
loglevel=warn
fund=false

# Keep the virtual store inside this workspace so Turbopack's `root` boundary
# can reach package files. The global virtual store symlinks node_modules out
# to ~/Library/pnpm/store, which Next.js 16 Turbopack rejects as outside-root.
enable-global-virtual-store=false
