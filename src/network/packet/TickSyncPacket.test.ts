import TickSyncPacket from './TickSyncPacket';

describe('network', () => {
    describe('packet', () => {
        describe.skip('TickSyncPacket', () => {
            it('decode', () => {});

            it('encode', () => {});
        });
    });
});
