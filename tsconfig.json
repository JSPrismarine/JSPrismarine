{
    "extends": "./tsconfig.build.json",

    "compilerOptions": {
        "baseUrl": ".",
        "paths": {
            "@jsprismarine/*": ["packages/*/src"]
        },
        "noEmit": true
    },

    "exclude": ["**/*.config.*", "**/*.stories.*", "**/*.test.*"]
}
